Config = {
    minRadius: 2,
    maxRadius: 20,
    interval: 5,
    minSpeed: -30,
    maxSpeed: 30,
    tickInterval: 50,
    circleSquareMin: 3
};
Config.minDiameter = Config.minRadius * 2;
Config.maxDiameter = Config.maxRadius * 2;